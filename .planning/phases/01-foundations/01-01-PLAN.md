---
phase: 01-foundations
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - mix.exs
  - lib/orkestra/projector/lifecycle.ex
  - test/orkestra/projector/lifecycle_test.exs
autonomous: true
requirements: [ERR-01, ERR-03]
must_haves:
  truths:
    - "Lifecycle.next_delay/2 returns base for attempt 0 and base*2 for attempt 1, capped at backoff_cap_ms"
    - "Lifecycle.classify/2 returns :retry while attempts < max_retries and :park when attempts >= max_retries"
    - "Lifecycle.should_halt?/2 returns true exactly when attempts are exhausted (>= max_retries)"
    - "ecto, ecto_sql, and postgrex are declared as optional deps so Plan 03 schemas can compile"
  artifacts:
    - path: "lib/orkestra/projector/lifecycle.ex"
      provides: "Pure retry/park/halt decision functions (D-04, D-05)"
      contains: "defmodule Orkestra.Projector.Lifecycle"
      min_lines: 40
    - path: "test/orkestra/projector/lifecycle_test.exs"
      provides: "Unit tests covering backoff, classification, halt (no I/O)"
      contains: "Orkestra.Projector.LifecycleTest"
    - path: "mix.exs"
      provides: "ecto/ecto_sql/postgrex optional deps"
      contains: ":ecto"
  key_links:
    - from: "test/orkestra/projector/lifecycle_test.exs"
      to: "lib/orkestra/projector/lifecycle.ex"
      via: "alias Orkestra.Projector.Lifecycle, calls next_delay/classify/should_halt?"
      pattern: "Orkestra.Projector.Lifecycle"
---

<objective>
Establish the dependency-free retry foundation and unblock the Ecto-dependent work: declare the `ecto`/`ecto_sql`/`postgrex` optional deps in `mix.exs`, and implement the pure `Orkestra.Projector.Lifecycle` module that classifies retry-vs-park, computes exponential backoff, and decides halt — all as pure return values with no I/O (D-05).

Purpose: ERR-01 (retry with bounded exponential backoff, configurable per projector) and ERR-03 (halt-after-exhaustion decision) are pure decisions the Phase 2 GenServer will consume. Adding the optional deps here is the sequencing point flagged in CONTEXT.md / RESEARCH.md: the Plan 03 Ecto schemas cannot compile without `ecto` on the path, so the dep declaration must land in Wave 1.
Output: `mix.exs` with three new optional deps, `lib/orkestra/projector/lifecycle.ex`, and its unit test file.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-foundations/01-CONTEXT.md
@.planning/phases/01-foundations/01-RESEARCH.md
@.planning/phases/01-foundations/01-PATTERNS.md
@.claude/CLAUDE.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add ecto, ecto_sql, postgrex as optional deps in mix.exs</name>
  <files>mix.exs</files>
  <read_first>
    - mix.exs — existing `deps/0` with the `optional: true` entries for `:amqp` (line 31) and `:spear` (line 34); the `application/0` function (lines 21-25) which must NOT change.
    - .planning/phases/01-foundations/01-PATTERNS.md — "mix.exs (modify — add optional deps)" section: exact entries and alphabetical-ordering guidance.
    - .planning/phases/01-foundations/01-RESEARCH.md — "Standard Stack" (version ranges) and "State of the Art" note that optional deps must NOT be added to `extra_applications`.
  </read_first>
  <action>
    Add three entries to the `deps/0` list in mix.exs, each with `optional: true`, mirroring the existing `:amqp`/`:spear` shape: `:ecto` at `~> 3.12`, `:ecto_sql` at `~> 3.12`, and `:postgrex` at `~> 0.18` (version ranges per RESEARCH.md Standard Stack, all VERIFIED on hex.pm). Place them grouped with the other optional deps. Do NOT modify `application/0` — per RESEARCH.md State of the Art, optional deps must not be listed in `extra_applications` on Elixir 1.8+. After editing, run `mix deps.get` so `ecto`/`ecto_sql`/`postgrex` are fetched onto the path; this is what lets the Plan 03 schema modules compile behind their `Code.ensure_loaded?(Ecto.Schema)` guards.
  </action>
  <verify>
    <automated>mix deps.get && mix compile --warnings-as-errors</automated>
  </verify>
  <acceptance_criteria>
    - `grep -E ':ecto,|:ecto_sql,|:postgrex,' mix.exs` shows all three with `optional: true`.
    - `application/0` is unchanged: `grep -A2 'def application' mix.exs` still lists only `extra_applications: [:logger]`.
    - `mix deps.get` succeeds and `ecto`, `ecto_sql`, `postgrex` appear in `mix.lock`.
    - `mix compile --warnings-as-errors` succeeds.
  </acceptance_criteria>
  <done>The three storage deps are declared optional, fetched into mix.lock, and the project compiles cleanly; `application/0` untouched.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Implement pure Orkestra.Projector.Lifecycle with tests</name>
  <files>lib/orkestra/projector/lifecycle.ex, test/orkestra/projector/lifecycle_test.exs</files>
  <read_first>
    - lib/orkestra/command_envelope.ex — the `retryable?/1` pattern (lines 91-97) showing the `attempts <= max_retries` boundary comparison, and the `@type t :: %{...}` / `@spec` / `@doc` conventions to mirror.
    - .planning/phases/01-foundations/01-RESEARCH.md — Pattern 5 (full Lifecycle reference: `next_delay/2`, `classify/2`, `should_halt?/2`, `@default_config`, the `import Bitwise, only: [bsl: 2]` inside the function body, and the matching unit-test sketch) and Pitfall 5 (integer-overflow guard).
    - .planning/phases/01-foundations/01-PATTERNS.md — "lib/orkestra/projector/lifecycle.ex" section and the "@doc + @spec on Every Public Function" shared pattern.
    - test/orkestra/metadata_test.exs — `use ExUnit.Case, async: true`, `alias`, one `describe` block per function, one `test` per boundary condition.
  </read_first>
  <behavior>
    - next_delay(0, config) == backoff_base_ms (e.g. 500)
    - next_delay(1, config) == backoff_base_ms * 2 (e.g. 1_000)
    - next_delay(2, config) == backoff_base_ms * 4 (e.g. 2_000)
    - next_delay(large_attempt, config) == backoff_cap_ms (capped, never overflows)
    - classify(attempts, config) == :retry when attempts < max_retries
    - classify(attempts, config) == :park when attempts >= max_retries
    - should_halt?(attempts, config) == true when attempts >= max_retries, false otherwise
    - all three functions work with the default config when called with arity 1
  </behavior>
  <action>
    Create the pure module `Orkestra.Projector.Lifecycle` (no `use`, no `@behaviour`, no I/O — per D-05). Define `@type config :: %{max_retries: non_neg_integer(), backoff_base_ms: non_neg_integer(), backoff_cap_ms: non_neg_integer()}` and a `@default_config` (pick defaults at your discretion per D-04; RESEARCH.md suggests max_retries 5, base 500ms, cap 30_000ms). Implement three public functions, each with a `@doc` and `@spec`:
      - `next_delay(attempt, config \\ @default_config)` — exponential backoff using `import Bitwise, only: [bsl: 2]` inside the function body so the formula is `min(cap, base * Bitwise.bsl(1, attempt))`. Apply the cap so the result never overflows for large attempts (RESEARCH.md Pitfall 5): clamp the shift amount or apply the cap such that `attempt` values beyond the practical `max_retries` still return `cap`. Do NOT use `:math.pow` (float rounding / overflow — RESEARCH.md "Don't Hand-Roll").
      - `classify(attempts, config \\ @default_config)` — returns `:retry` when `attempts < config.max_retries`, else `:park` (the ERR-01→ERR-03 decision; mirrors the `attempts <= max_retries` boundary style in command_envelope.ex but uses strict `<` so attempt == max_retries parks).
      - `should_halt?(attempts, config \\ @default_config)` — returns `true` when `attempts >= config.max_retries` (the ERR-03 halt decision).
    Then create the test file `Orkestra.Projector.LifecycleTest` with `use ExUnit.Case, async: true` (safe — pure functions, no shared state), one `describe` per function, asserting every boundary in the behavior block above including the cap/overflow case.
  </action>
  <verify>
    <automated>mix test test/orkestra/projector/lifecycle_test.exs --seed 0</automated>
  </verify>
  <acceptance_criteria>
    - `mix test test/orkestra/projector/lifecycle_test.exs --seed 0` passes with at least the boundary tests in the behavior block (attempt 0, 1, 2, cap; classify retry/park; halt true/false).
    - `grep -c '@spec' lib/orkestra/projector/lifecycle.ex` is >= 3 (one per public function).
    - `grep -c '@doc' lib/orkestra/projector/lifecycle.ex` is >= 3.
    - `grep -v '^#' lib/orkestra/projector/lifecycle.ex | grep -c ':math.pow'` is 0 (backoff uses Bitwise, not floats).
    - `mix format --check-formatted lib/orkestra/projector/lifecycle.ex test/orkestra/projector/lifecycle_test.exs` passes.
  </acceptance_criteria>
  <done>`Orkestra.Projector.Lifecycle` exposes pure `next_delay/2`, `classify/2`, `should_halt?/2`; all unit tests pass with no I/O; backoff is integer-only and capped; ERR-01 retry/backoff and ERR-03 halt decisions are verifiable.</done>
</task>

</tasks>

<artifacts_produced>
## Artifacts this plan produces

- Module `Orkestra.Projector.Lifecycle` (`lib/orkestra/projector/lifecycle.ex`)
  - Type `Orkestra.Projector.Lifecycle.config()`
  - `next_delay/2` (and `/1` via default config) — exponential backoff, integer, capped
  - `classify/2` (and `/1`) — returns `:retry | :park`
  - `should_halt?/2` (and `/1`) — returns `boolean()`
  - Module attribute `@default_config` (max_retries / backoff_base_ms / backoff_cap_ms)
- Test module `Orkestra.Projector.LifecycleTest` (`test/orkestra/projector/lifecycle_test.exs`)
- `mix.exs` deps additions: `{:ecto, "~> 3.12", optional: true}`, `{:ecto_sql, "~> 3.12", optional: true}`, `{:postgrex, "~> 0.18", optional: true}`
</artifacts_produced>

<threat_model>
## Trust Boundaries

This plan is pure library-internal logic (a pure-function module) plus a build-manifest change. There is no network, no auth, no user-input surface, and no process boundary crossed by untrusted data.

| Boundary | Description |
|----------|-------------|
| (none) | No trust boundary is crossed by this plan's code |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-01-01 | Denial of Service | `Lifecycle.next_delay/2` | mitigate | Unbounded backoff growth is prevented by always applying `min(cap, ...)` and clamping the shift so large `attempt` values cannot overflow the BEAM integer (RESEARCH.md Pitfall 5) |
| T-01-SC | Tampering | hex deps (`ecto`/`ecto_sql`/`postgrex`) | accept | All three are official `elixir-ecto` packages, verified on hex.pm in RESEARCH.md Package Legitimacy Audit (verdict OK); no `[ASSUMED]`/`[SUS]`/`[SLOP]` packages introduced, so no install checkpoint required |

Honest note (per security contribution): this is a dependency-free-logic + manifest phase; the threat surface is minimal by design. The only real control is the backoff cap.
</threat_model>

<verification>
- `mix deps.get` succeeds; `ecto`, `ecto_sql`, `postgrex` present in `mix.lock`.
- `mix compile --warnings-as-errors` succeeds.
- `mix test test/orkestra/projector/lifecycle_test.exs` passes.
- `mix format --check-formatted` passes for the touched files.
</verification>

<success_criteria>
- ROADMAP success criterion 3 (Lifecycle pure functions correctly classify errors, compute retry delays, and decide halt — verifiable via unit tests with no I/O) is satisfied for the retry/backoff/halt portion.
- ERR-01 (retry with bounded backoff, configurable per projector via the config map) is implemented as pure decisions.
- ERR-03 (halt-after-exhaustion decision) is implemented as a pure decision (the persisted halted flag lands in Plan 03 / Phase 2).
- Optional Ecto deps declared, unblocking Plan 03.
</success_criteria>

<output>
Create `.planning/phases/01-foundations/01-01-SUMMARY.md` when done.
</output>
