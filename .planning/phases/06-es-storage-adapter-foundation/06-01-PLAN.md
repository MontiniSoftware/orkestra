---
phase: 06-es-storage-adapter-foundation
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - mix.exs
  - lib/orkestra/auth/api_key.ex
  - test/support/es_cluster_mock.ex
  - test/orkestra/auth/api_key_test.exs
  - test/test_helper.exs
autonomous: true
requirements:
  - ADPT-03

must_haves:
  truths:
    - "Snap ~> 0.16 and Finch ~> 0.17 are declared as optional deps in mix.exs and resolve successfully"
    - "Mox ~> 1.0 is declared as a test-only dep in mix.exs"
    - "Orkestra.Auth.ApiKey implements Snap.Auth behaviour and adds Authorization: ApiKey header"
    - "ApiKey.sign/5 passes through unchanged when no :api_key config is present"
    - "Test cluster mock module and Mox mock are available for ES adapter tests"
  artifacts:
    - path: "lib/orkestra/auth/api_key.ex"
      provides: "Snap.Auth behaviour implementation for ES API key authentication"
      contains: "@behaviour Snap.Auth"
    - path: "test/support/es_cluster_mock.ex"
      provides: "Test Snap.Cluster module and Mox mock definition for Snap.HTTPClient"
      contains: "use Snap.Cluster"
    - path: "test/orkestra/auth/api_key_test.exs"
      provides: "Unit tests for ApiKey auth module"
      contains: "Orkestra.Auth.ApiKeyTest"
  key_links:
    - from: "lib/orkestra/auth/api_key.ex"
      to: "Snap.Auth behaviour"
      via: "@behaviour Snap.Auth and @impl Snap.Auth on sign/5"
      pattern: "@impl Snap.Auth"
    - from: "test/support/es_cluster_mock.ex"
      to: "Snap.HTTPClient"
      via: "Mox.defmock for HTTP client"
      pattern: "Mox.defmock"
---

<objective>
Add Snap and Finch as optional dependencies, implement the API key authentication module, and set up ES test infrastructure (mock cluster, Mox).

Purpose: Establishes the dependency foundation and auth support that the ES storage adapter (Plan 02) needs. Without Snap in mix.exs, the conditionally-compiled adapter module will not be defined. Without the Mox mock for Snap.HTTPClient, adapter tests cannot run without a live cluster.

Output: Working API key auth module with passing tests; mix deps resolve; test mock infrastructure ready for Plan 02.
</objective>

<execution_context>
@.claude/get-shit-done/workflows/execute-plan.md
@.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/06-es-storage-adapter-foundation/06-RESEARCH.md
@.planning/phases/06-es-storage-adapter-foundation/06-PATTERNS.md

<interfaces>
<!-- From lib/orkestra/projection/storage.ex (behaviour this phase implements): -->
@callback write(projector_name(), event(), non_neg_integer(), opts()) ::
            {:ok, ops()} | {:error, term()}
@callback reset(projector_name(), opts()) :: :ok | {:error, term()}

<!-- From lib/orkestra/projection/storage/postgres.ex (reference adapter pattern): -->
if Code.ensure_loaded?(Ecto.Multi) do
  defmodule Orkestra.Projection.Storage.Postgres do
    @behaviour Orkestra.Projection.Storage
    # write/4 uses Keyword.fetch!(opts, :handler), calls handler.(projector_name, event, position)
    # reset/2 uses Keyword.fetch!(opts, :repo) and :schema

<!-- Snap.Auth behaviour (from research, modelled on Snap.Auth.Plain): -->
@callback sign(config :: keyword(), method :: String.t(), url :: String.t(),
               headers :: [{String.t(), String.t()}], body :: String.t()) ::
            {:ok, {String.t(), String.t(), [{String.t(), String.t()}], String.t()}}

<!-- Existing optional dep pattern in mix.exs (lines 35-41): -->
{:amqp, "~> 4.1", optional: true},
{:ecto, "~> 3.12", optional: true},
{:ecto_sql, "~> 3.12", optional: true},
{:postgrex, "~> 0.18", optional: true},
{:spear, "~> 1.4", optional: true},

<!-- Existing test support pattern (test/support/projection_test_repo.ex): -->
defmodule Orkestra.Test.ProjectionRepo do
  @moduledoc false
  use Ecto.Repo, otp_app: :orkestra, adapter: Ecto.Adapters.Postgres
end

<!-- test/test_helper.exs current structure: -->
# Sets up PubSub, MessageBus config, Postgres sandbox
# Ends with ExUnit.start(exclude: [:postgres])
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add Snap, Finch, and Mox dependencies to mix.exs</name>
  <files>mix.exs</files>
  <read_first>
    - mix.exs
  </read_first>
  <action>
Add three new dependency entries to the `deps/0` function in mix.exs:

1. `{:snap, "~> 0.16", optional: true}` -- insert after the `{:spear, ...}` line (line 41), before `{:ex_doc, ...}`
2. `{:finch, "~> 0.17", optional: true}` -- insert immediately after the snap line
3. `{:mox, "~> 1.0", only: :test}` -- insert after the finch line (test-only, not optional)

Keep the existing deps unchanged. Preserve the formatting (2-space indent, trailing comma on each line).

After editing, run `mix deps.get` to resolve and lock the new dependencies.
  </action>
  <verify>
    <automated>cd /data/progetti/orkestra && mix deps.get && mix deps.compile snap finch mox --force 2>&1 | tail -5</automated>
  </verify>
  <acceptance_criteria>
    - mix.exs contains `{:snap, "~> 0.16", optional: true}`
    - mix.exs contains `{:finch, "~> 0.17", optional: true}`
    - mix.exs contains `{:mox, "~> 1.0", only: :test}`
    - `mix deps.get` exits 0 (dependencies resolve without conflict)
    - `mix deps.compile snap finch mox` exits 0
    - mix.lock contains entries for snap, finch, and mox
  </acceptance_criteria>
  <done>Snap 0.16.x, Finch 0.17.x, and Mox 1.x are locked and compiled. Conditional compilation guards for Snap.Cluster will now pass in test and dev environments.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Create Orkestra.Auth.ApiKey module and test</name>
  <files>lib/orkestra/auth/api_key.ex, test/orkestra/auth/api_key_test.exs</files>
  <read_first>
    - lib/orkestra/projection/storage/postgres.ex (for behaviour declaration and @impl pattern)
    - test/orkestra/projection/storage/postgres_test.exs (for test structure pattern)
  </read_first>
  <behavior>
    - Test 1: sign/5 adds `Authorization: ApiKey <encoded>` header when `:api_key` config is present
    - Test 2: sign/5 passes through method/url/headers/body unchanged when `:api_key` config is absent
    - Test 3: sign/5 passes through when `:api_key` is nil
    - Test 4: Orkestra.Auth.ApiKey declares @behaviour Snap.Auth (behaviour contract check)
    - Test 5: Snap.Auth.Plain.sign/5 adds `Authorization: Basic <encoded>` header when username+password config present (ADPT-03 Basic Auth coverage)
  </behavior>
  <action>
Create `lib/orkestra/auth/api_key.ex`:
- Wrap in `if Code.ensure_loaded?(Snap.Cluster) do ... end` guard
- Module: `Orkestra.Auth.ApiKey`
- `@moduledoc` explaining: this is a Snap.Auth implementation for ES/OpenSearch API key auth; the `:api_key` config value must be the already base64-encoded combined string (`Base.encode64("id:api_key")`); the resulting header is `Authorization: ApiKey <encoded>`
- `@behaviour Snap.Auth`
- `@impl Snap.Auth` on `sign/5`
- `sign(config, method, url, headers, body)` -- Keyword.fetch `:api_key` from config; if present and is_binary, prepend `{"Authorization", "ApiKey " <> encoded_key}` to headers list; if absent, pass through unchanged
- Return `{:ok, {method, url, updated_headers, body}}`

Create `test/orkestra/auth/api_key_test.exs`:
- Wrap in `if Code.ensure_loaded?(Snap.Cluster) do ... end` guard (module only exists when Snap is available)
- Module: `Orkestra.Auth.ApiKeyTest`
- `@moduledoc false`
- `use ExUnit.Case, async: true` (no shared state)
- Tests per the behavior block above
- For behavior contract test: check `Orkestra.Auth.ApiKey.__info__(:attributes) |> Keyword.get_values(:behaviour) |> List.flatten()` includes `Snap.Auth`
- Test 5 (Basic Auth via Snap.Auth.Plain): call `Snap.Auth.Plain.sign([username: "elastic", password: "changeme"], "GET", "http://localhost:9200/", [], "")` and assert the returned headers include an `"Authorization"` header starting with `"Basic "`. This verifies ADPT-03 Basic Auth branch without a live cluster.
  </action>
  <verify>
    <automated>cd /data/progetti/orkestra && mix test test/orkestra/auth/api_key_test.exs --no-deps-check</automated>
  </verify>
  <acceptance_criteria>
    - lib/orkestra/auth/api_key.ex exists and compiles
    - Module declares `@behaviour Snap.Auth`
    - sign/5 with config `[api_key: "dGVzdDprZXk="]` returns headers containing `{"Authorization", "ApiKey dGVzdDprZXk="}`
    - sign/5 with empty config `[]` returns original headers unchanged
    - All tests in api_key_test.exs pass (including Basic Auth via Snap.Auth.Plain test)
  </acceptance_criteria>
  <done>Orkestra.Auth.ApiKey module implements Snap.Auth behaviour, correctly injects API key header, and all 4 tests pass.</done>
</task>

<task type="auto">
  <name>Task 3: Create ES test infrastructure (mock cluster, Mox mock, test_helper updates)</name>
  <files>test/support/es_cluster_mock.ex, test/test_helper.exs</files>
  <read_first>
    - test/test_helper.exs
    - test/support/projection_test_repo.ex (for test support module pattern)
  </read_first>
  <action>
Create `test/support/es_cluster_mock.ex` with two conditional blocks:

Block 1 -- Test Snap.Cluster module (guarded by `if Code.ensure_loaded?(Snap.Cluster) do`):
- `Orkestra.Test.ESCluster` module with `@moduledoc false` and `use Snap.Cluster, otp_app: :orkestra`

Block 2 -- Mox mock definition (guarded by `if Code.ensure_loaded?(Snap.HTTPClient) do`):
- `Mox.defmock(Snap.MockHTTPClient, for: Snap.HTTPClient)`

Update `test/test_helper.exs`:
- After the existing Postgres sandbox block (line 33, before the final `ExUnit.start`), add a new conditional block for ES test config:
  ```
  if Code.ensure_loaded?(Snap.Cluster) do
    Application.put_env(:orkestra, Orkestra.Test.ESCluster,
      url: "http://localhost:9200",
      http_client_adapter: Snap.MockHTTPClient
    )
  end
  ```
- Update the final `ExUnit.start(exclude: [:postgres])` to also exclude `:elasticsearch` and `:integration`:
  `ExUnit.start(exclude: [:postgres, :elasticsearch, :integration])`

This ensures ES tests that need a live cluster are excluded by default (same pattern as Postgres tests), while unit tests using Mox mocks run normally.
  </action>
  <verify>
    <automated>cd /data/progetti/orkestra && mix compile --no-deps-check --warnings-as-errors 2>&1 | tail -5 && mix test --no-deps-check --max-failures 1 2>&1 | tail -5</automated>
  </verify>
  <acceptance_criteria>
    - test/support/es_cluster_mock.ex exists and compiles without warnings
    - Orkestra.Test.ESCluster module is defined when Snap is available
    - Snap.MockHTTPClient mock is defined when Snap.HTTPClient is available
    - test/test_helper.exs configures Orkestra.Test.ESCluster with mock HTTP adapter
    - ExUnit.start excludes :postgres, :elasticsearch, and :integration tags
    - All existing tests still pass (no regression)
  </acceptance_criteria>
  <done>ES test infrastructure is ready: mock cluster module, Mox HTTP client mock, and test_helper config. Plan 02 can write adapter tests against this infrastructure.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Library config -> Snap.Auth.sign/5 | API key credential flows from application config into HTTP headers |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-06-01 | Information Disclosure | Orkestra.Auth.ApiKey | mitigate | ApiKey expects pre-encoded base64 string; moduledoc warns against committing credentials to git; recommend runtime config + secrets manager |
| T-06-02 | Tampering | Orkestra.Auth.ApiKey.sign/5 | accept | ApiKey is a pass-through; if config is wrong, ES returns 401 -- no data corruption risk |
</threat_model>

<verification>
1. `mix deps.get` resolves without conflict
2. `mix compile --warnings-as-errors` succeeds
3. `mix test test/orkestra/auth/api_key_test.exs` -- all ApiKey tests pass
4. `mix test` -- full suite passes (no regression from new deps or test_helper changes)
</verification>

<success_criteria>
- Snap 0.16.x, Finch 0.17.x, and Mox 1.x are in mix.lock and compile
- Orkestra.Auth.ApiKey implements Snap.Auth with API key header injection
- Test infrastructure (mock cluster, Mox mock, test_helper config) is ready for Plan 02
- All existing tests pass unchanged
</success_criteria>

<output>
After completion, create `.planning/phases/06-es-storage-adapter-foundation/06-01-SUMMARY.md`
</output>
