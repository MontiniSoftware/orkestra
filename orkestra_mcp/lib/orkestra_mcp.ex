defmodule OrkestraMcp do
  @moduledoc """
  MCP server for scaffolding and introspecting Elixir projects built with Orkestra.

  OrkestraMcp exposes:

  - **Tools** for generating Commands, Events, Handlers, and Aggregates
  - **Resources** for introspecting an existing project's domain model
  - **Prompts** with Orkestra conventions and guided workflows
  """
end
